## Servidor REST básico utilizando Express

### Instalación
1. **Instalar Express, dotenv**

    ```bash
    pnpm add express dotenv
    ```

2.  **Instalar las definiciones de tipos de Express para TypeScript:**

    ```bash
    pnpm add -D @types/express
    ```
